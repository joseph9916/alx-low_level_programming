#include "main.h"

/**
 * mul - Find the multiplication of two numbers
 *
 * @a: first number
 * @b: Second number
 * Return: multiplication result
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
